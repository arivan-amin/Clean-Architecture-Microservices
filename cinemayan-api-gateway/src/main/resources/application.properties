spring.application.name = api-gateway
server.port = 8080

logging.level.org.springframework.cloud.gateway = info

# ===================================
# BANNER PROPERTIES
# ===================================
project-name = @project.parent.artifactId@
service-name = @project.artifactId@
service-version = @project.version@

# ===================================
# EUREKA
# ===================================
lms.eureka.host = ${EUREKA_HOST:localhost}
lms.eureka.port = ${EUREKA_PORT:8761}
eureka.client.serviceUrl.defaultZone = http://${EUREKA_USERNAME:admin}:${EUREKA_PASSWORD:admin}@${EUREKA_HOST:localhost}:${EUREKA_PORT:8761}/eureka

# ===================================
# OBSERVABILITY
# ===================================
info.app.name = API Gateway
info.app.description = API Gateway Service
info.app.version = 1.0
management.observations.key-values.application = ${spring.application.name}
management.endpoints.web.exposure.include = *
management.tracing.sampling.probability = 1.0
management.info.build.enabled = true
management.info.git.enabled = true
management.info.env.enabled = true
management.endpoint.health.show-details = always
management.metrics.distribution.percentiles-histogram.http.server.requests = true
server.tomcat.mbeanregistry.enabled = true
management.endpoint.health.probes.enabled = true
management.opentelemetry.tracing.export.otlp.endpoint = http://${OPEN_TELEMETRY_HOST:localhost}:4318/v1/traces
management.opentelemetry.metrics.export.otlp.endpoint = http://${OPEN_TELEMETRY_HOST:localhost}:4318/v1/metrics

# ===================================
# AUDIT EVENT SENSITIVE FIELDS
# ===================================
audit.masking.sensitive.fields[0] = password
audit.masking.sensitive.fields[1] = pin
audit.masking.sensitive.fields[2] = cvv
audit.masking.sensitive.fields[3] = cardNumber
audit.masking.sensitive.fields[4] = ssn
audit.masking.sensitive.fields[5] = secretKey
audit.masking.sensitive.fields[6] = token

# ===================================
# ACTUATOR
# ===================================
management.endpoints.web.base-path = /actuator/api-gateway

# ===================================
# SWAGGER UI AGGREGATE SETUP
# ===================================
spring.main.web-application-type = reactive
springdoc.api-docs.enabled = true
springdoc.swagger-ui.enabled = true
springdoc.swagger-ui.path = /swagger-ui.html

# ===================================
# SERVICES ROUTES
# ===================================
cinemayan.api.gateway.service.routes[0].name = catalog
cinemayan.api.gateway.service.routes[0].pathPrefix = catalogs
cinemayan.api.gateway.service.routes[0].actuatorEnabled = true
cinemayan.api.gateway.service.routes[0].apiDocEnabled = true
cinemayan.api.gateway.service.routes[0].rateLimiterEnabled = true

cinemayan.api.gateway.service.routes[1].name = audit
cinemayan.api.gateway.service.routes[1].pathPrefix = audits
cinemayan.api.gateway.service.routes[1].actuatorEnabled = true
cinemayan.api.gateway.service.routes[1].apiDocEnabled = false
cinemayan.api.gateway.service.routes[1].rateLimiterEnabled = true

# ===================================
# CATALOG SWAGGER UI ROUTE
# ===================================
springdoc.swagger-ui.urls[0].name = Catalog Service
springdoc.swagger-ui.urls[0].url = /catalog-service/api-docs

# ===================================
# AUDIT SWAGGER UI ROUTE
# ===================================
springdoc.swagger-ui.urls[1].name = Audit Service
springdoc.swagger-ui.urls[1].url = /audit-service/api-docs
